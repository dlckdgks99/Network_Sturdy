# 11. 패킷 캡쳐 프로그래밍

# 11.1 tcpdump

### tcpdump 사용 예
### tcpdump 명령 인자
- 옵션 설정
- 필터링 규칙 설정
- 특정 호스트 또는 포트 지정

# 11.2 pcap 라이브러리 주요 함수
### 네트워크 디바이스 열기


### 패킷 캡쳐 함수
- pcap_dispatch()

# 11.3 패킷 캡쳐 예제
- pcap_loop() 사용 예
- pcap_dispatch() 사용 예
- pcap_next() 사용 예
- 패킷 필터링

# 11.4 TCP/IP 헤더 캡쳐 예
- IP 헤더 출력
- TCP 헤더 출력
- UDP 헤더 출력
